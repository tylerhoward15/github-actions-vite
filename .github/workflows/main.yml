on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: get current directory
        run: pwd
      - name: print readme
        run: cat ./README.md
      - name: check localhost
        run: curl localhost:5173
